export interface IBook {
  id: string;
  bookName: string;
  releaseDate: string;
  description: string;
  pageCount: number;
  author: string;
  Rating: number;
}//replace these with the ones i have on the api so no image for example
