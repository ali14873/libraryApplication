export interface IBook {
  id: string;
  name: string;
  releaseDate: string;
  description: string;
  pageCount: number;
  author: string;
  rating: number;
}//replace these with the ones i have on the api so no image for example
